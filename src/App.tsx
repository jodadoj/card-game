//const Card = require('./card.js');

function App() {
  return;
}

export default App;

//attempted to add husky to package.json, check check later

// "husky": {
//     "hooks": {
//       "pre-commit": "yarn prettier --write . && yarn eslint",
//       "pre-push": "yarn prettier --write . && yarn eslint"
//     }
//   }
